# Backend API base URL (include /api suffix)
# Set this in Vercel Project Settings > Environment Variables.
# Include the /api suffix and do not add a trailing slash.
NEXT_PUBLIC_API_URL=https://hrm-lilac-one.vercel.app/api
